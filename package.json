{
  "name": "locksmith-social-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Social media posting agent for Home Advisor Locksmith. Runs on GitHub Actions cron. Posts curated content to Google Business Profile + Instagram + Facebook via upload-post.com. Supports multiple Google Business locations under one connected account via GBP_LOCATION_ID.",
  "scripts": {
    "post": "tsx src/index.ts",
    "post:dry": "tsx src/index.ts --dry-run",
    "post:next": "tsx src/index.ts --print-next",
    "gbp:locations": "tsx src/locations.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.70.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
